; Per-application PHP config for the `admin` app, selected via
; options.file = /www/admin/php.ini in config.json. A deliberately different
; memory_limit from the site app (256M via options.admin) makes the per-app
; isolation visible: GET /admin/ reports 512M, GET / reports 256M.
memory_limit = 512M
